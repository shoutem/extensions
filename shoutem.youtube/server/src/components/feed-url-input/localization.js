const key = 'feed-url-input';

const TOOLTIP_LABEL = `${key}.tooltip-label`;
const INPUT_TITLE = `${key}.input-title`;
const CONTINUE_BUTTON = `${key}.continue-button`;

export default {
  TOOLTIP_LABEL,
  INPUT_TITLE,
  CONTINUE_BUTTON,
};
