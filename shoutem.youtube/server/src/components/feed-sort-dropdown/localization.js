const key = 'feed-sort-dropdown';

const SORT_BY_LABEL = `${key}.sort-by-label`;
const SORT_OPTIONS_DESCRIPTIONS = `${key}.sort-options-descriptions`;

export default {
  SORT_BY_LABEL,
  SORT_OPTIONS_DESCRIPTIONS,
};
