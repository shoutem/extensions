const key = 'deals-statistics-page';

const TITLE = `${key}.title`;
const SEARCH_PLACEHOLDER_MESSAGE = `${key}.search-placeholder-message`;

export default {
  TITLE,
  SEARCH_PLACEHOLDER_MESSAGE,
};
