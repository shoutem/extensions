const key = 'services';

const EMPTY_DEALS_ENDPOINT_TITLE = `${key}.empty-deals-endpoint-title`;
const DISPLAY_DATE_FORMAT = `${key}.display-date-format`;
const DISPLAY_TIME_FORMAT = `${key}.display-time-format`;

export default {
  EMPTY_DEALS_ENDPOINT_TITLE,
  DISPLAY_DATE_FORMAT,
  DISPLAY_TIME_FORMAT,
};
