const key = 'transactions-stats-dashboard';

const TITLE = `${key}.title`;
const TRANSACTION_DISPLAY_TIME = `${key}.transaction-display-time`;
const HEADER_TIME_TITLE = `${key}.header-time-title`;
const HEADER_USER_TITLE = `${key}.header-user-title`;
const HEADER_TYPE_TITLE = `${key}.header-type-title`;
const HEADER_PLACE_TITLE = `${key}.header-place-title`;
const ACTION_COUPON_CLAIMED_TITLE = `${key}.action-coupon-claimed-title`;
const ACTION_COUPON_REDEEMED_TITLE = `${key}.action-coupon-redeemed-title`;
const ACTION_DEAL_REDEEMED_TITLE = `${key}.action-deal-redeemed-title`;
const TABLE_EMPTY_PLACEHOLDER_MESSAGE = `${key}.table-empty-placeholder-message`;

export default {
  TITLE,
  TRANSACTION_DISPLAY_TIME,
  HEADER_TIME_TITLE,
  HEADER_USER_TITLE,
  HEADER_TYPE_TITLE,
  HEADER_PLACE_TITLE,
  ACTION_COUPON_CLAIMED_TITLE,
  ACTION_COUPON_REDEEMED_TITLE,
  ACTION_DEAL_REDEEMED_TITLE,
  TABLE_EMPTY_PLACEHOLDER_MESSAGE,
};
