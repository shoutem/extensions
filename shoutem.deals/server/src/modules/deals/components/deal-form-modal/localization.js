const key = 'deal-form-modal';

const ERROR_MESSAGE = `${key}.error-message`;
const MODAL_EDIT_TITLE = `${key}.modal-edit-title`;
const MODAL_ADD_TITLE = `${key}.modal-add-title`;

export default {
  ERROR_MESSAGE,
  MODAL_EDIT_TITLE,
  MODAL_ADD_TITLE,
};
