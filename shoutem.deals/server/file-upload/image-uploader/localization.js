const key = 'image-uploader';

const MAX_SIZE_MESSAGE = `${key}.max-size-message`;
const UPLOAD_FAILED_MESSAGE = `${key}.upload-failed-message`;
const FILE_REJECTED_MESSAGE = `${key}.file-rejected-message`;
const DELETE_FAILED_MESSAGE = `${key}.delete-failed-message`;

export default {
  MAX_SIZE_MESSAGE,
  UPLOAD_FAILED_MESSAGE,
  FILE_REJECTED_MESSAGE,
  DELETE_FAILED_MESSAGE,
};
