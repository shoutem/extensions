const key = 'cms-dashboard.filters';

const OPEATOR_EQUALS = `${key}.operator-equals`;
const OPEATOR_LESS_THAN = `${key}.operator-less-than`;
const OPEATOR_GREATER_THAN = `${key}.operator-greater-than`;
const OPEATOR_CONTAINS = `${key}.operator-contains`;
const SELECT_CATEGORY = `${key}.select-category`;
const RULE = `${key}.rule`;
const TYPE_IN_VALUE = `${key}.type-in-value`;

export default {
  OPEATOR_EQUALS,
  OPEATOR_LESS_THAN,
  OPEATOR_GREATER_THAN,
  OPEATOR_CONTAINS,
  SELECT_CATEGORY,
  RULE,
  TYPE_IN_VALUE,
};
