const key = 'barcode-reqex-form';

const REGULAR_EXPRESSION_TITLE = `${key}.regular-expression-title`;
const HELP_MESSAGE = `${key}.help-message`;
const REGEX_EXAMPLE = `${key}.regex-example`;
const BUTTON_SAVE_TITLE = `${key}.button-save-title`;

export default {
  REGULAR_EXPRESSION_TITLE,
  HELP_MESSAGE,
  REGEX_EXAMPLE,
  BUTTON_SAVE_TITLE,
};
