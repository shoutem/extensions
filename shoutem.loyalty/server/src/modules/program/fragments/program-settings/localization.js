const key = 'program-settings';

const TITLE = `${key}.title`;
const REQUIRE_RECEIPT = `${key}.require-receipt`;
const ENABLE_BARCODE_SCANNING = `${key}.enable-barcode-scanning`;

export default {
  TITLE,
  REQUIRE_RECEIPT,
  ENABLE_BARCODE_SCANNING,
};
