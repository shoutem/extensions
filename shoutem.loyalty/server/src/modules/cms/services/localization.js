const key = 'cms-services';

const ENDPOINT_REQUIRED_MESSAGE = `${key}.endpoint-required-message`;
const CANNOT_CONNECT_MESSAGE = `${key}.cannot-connect-message`;

export default {
  ENDPOINT_REQUIRED_MESSAGE,
  CANNOT_CONNECT_MESSAGE,
};
