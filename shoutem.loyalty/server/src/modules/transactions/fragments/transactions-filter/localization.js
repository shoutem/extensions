const key = 'transactions-filter';

const FILTER_BY_USER_PLACEHOLDER_MESSAGE = `${key}.filter-by-user-placeholder-message`;
const FILTER_BY_CARD_PLACEHOLDER_MESSAGE = `${key}.filter-by-card-placeholder-message`;
const FILTER_BY_PLACE_PLACEHOLDER_MESSAGE = `${key}.filter-by-place-placeholder-message`;
const FILTER_BY_CASHIER_PLACEHOLDER_MESSAGE = `${key}.filter-by-cashier-placeholder-message`;

export default {
  FILTER_BY_USER_PLACEHOLDER_MESSAGE,
  FILTER_BY_CARD_PLACEHOLDER_MESSAGE,
  FILTER_BY_PLACE_PLACEHOLDER_MESSAGE,
  FILTER_BY_CASHIER_PLACEHOLDER_MESSAGE,
};
