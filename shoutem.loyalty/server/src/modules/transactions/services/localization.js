const key = 'transactions-services';

const POINTS_MISSING_MESSAGE = `${key}.points-missing-message`;
const ONLY_NUMBER_ALLOWED_MESSAGE = `${key}.only-numbers-allowed-message`;
const USER_MISSING_MESSAGE = `${key}.user-missing-message`;
const PLACE_MISSING_MESSAGE = `${key}.place-missing-message`;
const PUNCH_MISSING_MESSAGE = `${key}.punch-missing-message`;

export default {
  POINTS_MISSING_MESSAGE,
  ONLY_NUMBER_ALLOWED_MESSAGE,
  USER_MISSING_MESSAGE,
  PLACE_MISSING_MESSAGE,
  PUNCH_MISSING_MESSAGE,
};
