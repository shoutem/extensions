const key = 'general-stats';

const PUNCHES_EARNED_TITLE = `${key}.punches-earned-title`;
const POINTS_EARNED_TITLE = `${key}.points-earned-title`;
const PUNCHES_REDEEMED_TITLE = `${key}.punches-redeemed-title`;
const POINTS_REDEEMED_TITLE = `${key}.points-redeemed-title`;
const REWARDS_REDEEMED_TITLE = `${key}.rewards-redeemed-title`;
const CURRENT_BALANCE_TITLE = `${key}.current-balance-title`;

export default {
  PUNCHES_EARNED_TITLE,
  POINTS_EARNED_TITLE,
  PUNCHES_REDEEMED_TITLE,
  POINTS_REDEEMED_TITLE,
  REWARDS_REDEEMED_TITLE,
  CURRENT_BALANCE_TITLE,
};
