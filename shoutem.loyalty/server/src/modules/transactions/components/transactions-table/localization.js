const key = 'transaction-table';

const EMPTY_PLACEHOLDER_TITLE = `${key}.empty-placeholder-title`;
const HEADER_USER_TITLE = `${key}.header-user-title`;
const HEADER_CARD_TITLE = `${key}.header-card-title`;
const HEADER_PLACE_TITLE = `${key}.header-place-title`;
const HEADER_CASHIER_TITLE = `${key}.header-cashier-title`;
const HEADER_TIME_TITLE = `${key}.header-time-title`;
const HEADER_TRANSACTION_TITLE = `${key}.header-transaction-title`;

export default {
  EMPTY_PLACEHOLDER_TITLE,
  HEADER_USER_TITLE,
  HEADER_CARD_TITLE,
  HEADER_PLACE_TITLE,
  HEADER_CASHIER_TITLE,
  HEADER_TIME_TITLE,
  HEADER_TRANSACTION_TITLE,
};
