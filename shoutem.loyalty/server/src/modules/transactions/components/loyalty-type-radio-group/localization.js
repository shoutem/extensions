const key = 'loyalty-type-radio-group';

const TYPE_OF_LOYALTY_TITLE = `${key}.type-of-loyalty-title`;
const POINTS_CARD_TITLE = `${key}.points-card-title`;
const PUNCH_CARD_TITLE = `${key}.punch-card-title`;
const MULTICARD_TITLE = `${key}.multicard-title`;

export default {
  TYPE_OF_LOYALTY_TITLE,
  POINTS_CARD_TITLE,
  PUNCH_CARD_TITLE,
  MULTICARD_TITLE,
};
