const key = 'rules-table';

const VISIT_ACTION_TITLE = `${key}.visit-action-title`;
const VISIT_UNIT_TITLE = `${key}.visit-unit-title`;
const LINEAR_POINT_ACTION_TITLE = `${key}.linear-point-action-title`;
const LINEAR_POINT_UNIT_TITLE = `${key}.linear-point-unit-title`;
const HEADER_CUSTOM_ACTION_TITLE = `${key}.header-custom-action-title`;
const HEADER_VALUE_TITLE = `${key}.header-value-title`;
const HEADER_RULE_ENABLED_TITLE = `${key}.header-rule-enabled-title`;

export default {
  VISIT_ACTION_TITLE,
  VISIT_UNIT_TITLE,
  LINEAR_POINT_ACTION_TITLE,
  LINEAR_POINT_UNIT_TITLE,
  HEADER_CUSTOM_ACTION_TITLE,
  HEADER_VALUE_TITLE,
  HEADER_RULE_ENABLED_TITLE,
};
