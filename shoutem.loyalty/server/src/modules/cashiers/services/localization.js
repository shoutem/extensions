const key = 'cashier-services';

const REQUIRED_FIELD_MESSAGE = `${key}.required-field-message`;
const EMAIL_REQUIRED_MESSAGE = `${key}.email-required-message`;
const EMAIL_NOT_VALID_MESSAGE = `${key}.email-not-valid-message`;
const PASSWORD_REQUIRED_MESSAGE = `${key}.password-required-message`;
const PASSWORD_TOO_SHORT_MESSAGE = `${key}.password-too-short-message`;
const PIN_UNIQUE_MESSAGE = `${key}.pin-unique-message`;
const USER_ALREADY_EXISTS_MESSAGE = `${key}.user-already-exists-message`;
const ERROR_MESSAGE = `${key}.error-message`;

export default {
  REQUIRED_FIELD_MESSAGE,
  EMAIL_REQUIRED_MESSAGE,
  EMAIL_NOT_VALID_MESSAGE,
  PASSWORD_REQUIRED_MESSAGE,
  PASSWORD_TOO_SHORT_MESSAGE,
  PIN_UNIQUE_MESSAGE,
  USER_ALREADY_EXISTS_MESSAGE,
  ERROR_MESSAGE,
};
