const key = 'loyalty-disabled-placeholder';

const TITLE = `${key}.title`;
const DESCRIPTION = `${key}.description`;
const BUTTON_TRY_AGAIN_TITLE = `${key}.button-try-again-title`;
const BUTTON_GET_STARTED_TITLE = `${key}.button-get-started-title`;
const FAILED_TO_ENABLE_MESSAGE = `${key}.failed-to-enable-message`;

export default {
  TITLE,
  DESCRIPTION,
  BUTTON_TRY_AGAIN_TITLE,
  BUTTON_GET_STARTED_TITLE,
  FAILED_TO_ENABLE_MESSAGE,
};
