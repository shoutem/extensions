const camera = {
  android: {
    gradle: {
      app: {
        defaultConfig:
          "missingDimensionStrategy 'react-native-camera', 'general'",
      },
    },
  },
};

module.exports = {
  camera,
};
