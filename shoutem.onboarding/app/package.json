{
  "name": "shoutem.onboarding",
  "version": "4.0.0",
  "description": "Shows a quick tour on the first app launch. ",
  "scripts": {
    "prebuild": "node -e 'require(\"./build\").runPreBuild()'"
  }
}
