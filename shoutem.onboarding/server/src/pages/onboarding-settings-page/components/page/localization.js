const key = 'onboarding-components-page';

const PAGE_DESCRIPTION = `${key}.page-description`;
const PAGE_IMAGE = `${key}.page-image`;
const PAGE_IMAGE_SIZE = `${key}.page-image-size`;
const FEATURED_IMAGE = `${key}.page-featured-image`;
const FEATURED_IMAGE_SIZE = `${key}.page-featured-image-size`;
const PAGE_TITLE = `${key}.page-title`;
const DELETE_TITLE = `${key}.delete-title`;
const DELETE_MESSAGE = `${key}.delete-message`;
const UPLOAD_FAILED_MESSAGE = `${key}.upload-fail-message`;
const DELETE_BTN_LABEL = `${key}.delete-btn-label`;
const DELETE_BTN_MESSAGE = `${key}.delete-btn-message`;
const CANCEL_BTN_LABEL = `${key}.cancel-btn-label`;
const TEXT_POSITION_LABEL = `${key}.text-position-label`;

export default {
  CANCEL_BTN_LABEL,
  DELETE_TITLE,
  DELETE_MESSAGE,
  DELETE_BTN_LABEL,
  DELETE_BTN_MESSAGE,
  FEATURED_IMAGE,
  FEATURED_IMAGE_SIZE,
  PAGE_DESCRIPTION,
  PAGE_IMAGE,
  PAGE_IMAGE_SIZE,
  PAGE_TITLE,
  UPLOAD_FAILED_MESSAGE,
  TEXT_POSITION_LABEL,
};
