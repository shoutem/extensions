const key = 'onboarding-components-empty-state-settings-page';

const TITLE = `${key}.title`;
const MESSAGE = `${key}.message`;
const BUTTON_LABEL = `${key}.button-label`;
const ERROR_MESSAGE = `${key}.error-message`;

export default {
  TITLE,
  MESSAGE,
  BUTTON_LABEL,
  ERROR_MESSAGE,
};
