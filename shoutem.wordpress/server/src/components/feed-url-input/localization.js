const key = 'feed-url-input';

const NOT_WORDPRESS_URL = `${key}.not-wordpress-url`;
const INVALID_URL = `${key}.invalid-url`;
const PAGE_URL = `${key}.page-url`;
const WORDPRESS_VERSION_WARNING = `${key}.wordpress-version-warning`;
const CONTINUE_BUTTON = `${key}.continue-button`;

export default {
  NOT_WORDPRESS_URL,
  INVALID_URL,
  PAGE_URL,
  WORDPRESS_VERSION_WARNING,
  CONTINUE_BUTTON,
};
