const key = 'settings-services';

const VALUE_REQUIRED = `${key}.value-required`;
const GOOGLE_SERVICES_REQUIRED = `${key}.google-services-required`;

export default {
  VALUE_REQUIRED,
  GOOGLE_SERVICES_REQUIRED,
};
