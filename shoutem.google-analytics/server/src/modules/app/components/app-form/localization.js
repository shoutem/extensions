const key = 'app-form';

const PROPERTY_ID = `${key}.property-id`;
const SERVICE_ACCOUNT_KEY_JSON = `${key}.service-account-key-json`;
const REQUIRED_FIELD_MESSAGE = `${key}.required-field-message`;
const SAVE = `${key}.save`;
const ERROR_MESSAGE = `${key}.error-message`;

export default {
  PROPERTY_ID,
  SERVICE_ACCOUNT_KEY_JSON,
  REQUIRED_FIELD_MESSAGE,
  SAVE,
  ERROR_MESSAGE,
};
