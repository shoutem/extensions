const key = 'rss-page';

const FORM_LOADING_FROM = `${key}.form-loading-form`;
const HEADER_DATE = `${key}.header-date`;
const HEADER_TITLE = `${key}.header-title`;
const LOADING_CONTENT = `${key}.loading-content`;
const NO_CONTENT = `${key}.no-content`;
const FORM_AVAILABLE_FEEDS = `${key}.form-available-feeds`;
const BUTTON_ADD_FEED = `${key}.button-add-feed`;
const BUTTON_CANCEL = `${key}.button-cancel`;
const INVALID_URL = `${key}.invalid-url`;
const FORM_FEED = `${key}.form-feed`;
const BUTTON_CONTINUE = `${key}.button-continue`;
const INVALID_RSS_FEED = `${key}.invalid-rss-feed`;
const FORM_SHOW_FEED = `${key}.form-show-feed`;

export default {
  FORM_LOADING_FROM,
  HEADER_DATE,
  HEADER_TITLE,
  LOADING_CONTENT,
  NO_CONTENT,
  FORM_AVAILABLE_FEEDS,
  BUTTON_ADD_FEED,
  BUTTON_CANCEL,
  INVALID_URL,
  FORM_FEED,
  BUTTON_CONTINUE,
  INVALID_RSS_FEED,
  FORM_SHOW_FEED,
};
