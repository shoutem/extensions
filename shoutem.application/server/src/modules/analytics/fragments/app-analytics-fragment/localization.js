const key = 'app-analytics';

const TITLE = `${key}.title`;
const ANALYTICS_TABLE_TITLE = `${key}.analytics-table-title`;
const ERROR_MESSAGE = `${key}.error-message`;

export default {
  TITLE,
  ANALYTICS_TABLE_TITLE,
  ERROR_MESSAGE,
};
