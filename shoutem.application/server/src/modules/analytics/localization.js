const key = 'analytics-constants';

const DOWNLOADS = `${key}.downloads`;
const SESSIONS = `${key}.sessions`;
const ACTIVE_USERS = `${key}.active-users`;
const PAGE_VIEWS = `${key}.page-views`;
const TODAY = `${key}.today`;
const YESTERDAY = `${key}.yesterday`;
const LAST_7_DAYS = `${key}.last_7_days`;
const LAST_30_DAYS = `${key}.last_30_days`;
const CUSTOM_RANGE = `${key}.custom_range`;

const CONSTANTS = {
  DOWNLOADS,
  SESSIONS,
  ACTIVE_USERS,
  PAGE_VIEWS,
  TODAY,
  YESTERDAY,
  LAST_7_DAYS,
  LAST_30_DAYS,
  CUSTOM_RANGE,
};

export default {
  CONSTANTS,
};
