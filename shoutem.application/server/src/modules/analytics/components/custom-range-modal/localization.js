const key = 'analytics.custom-range-modal';

const TITLE = `${key}.title`;
const FROM = `${key}.from`;
const TO = `${key}.to`;
const CANCEL = `${key}.cancel`;
const APPLY = `${key}.apply`;
const RANGE_DURATION_ERROR_MESSAGE = `${key}.range-duration-error-message`;

export default {
  TITLE,
  FROM,
  TO,
  CANCEL,
  APPLY,
  RANGE_DURATION_ERROR_MESSAGE,
};
