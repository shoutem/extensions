const key = 'cms-dashboard';

const SEARCH = `${key}.search.search`;
const ADD_FILTER = `${key}.filters.add-filter`;
const EDIT_FILTER = `${key}.filters.edit-filter`;

export default {
  SEARCH,
  ADD_FILTER,
  EDIT_FILTER,
};
