const key = 'cms-dashboard.filters';

const CREATE_FILTER = `${key}.create-filter`;
const RESET = `${key}.reset`;
const CANCEL = `${key}.cancel`;
const APPLY = `${key}.apply`;

export default {
  CREATE_FILTER,
  RESET,
  CANCEL,
  APPLY,
};
