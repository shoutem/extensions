const key = 'cms-dashboard.category-selector';

const EMPTY_PLACEHOLDER_LABEL = `${key}.empty-placeholder-label`;
const SELECT_ALL_LABEL = `${key}.select-all-label`;
const SELECT_NONE_LABEL = `${key}.select-none-label`;
const SELECT_LABEL = `${key}.select-label`;

export default {
  EMPTY_PLACEHOLDER_LABEL,
  SELECT_ALL_LABEL,
  SELECT_NONE_LABEL,
  SELECT_LABEL,
};
