const key = 'importer-csv-mapping-form';

const FORM_TITLE = `${key}.form-title`;
const HEADER_CMS_FIELD_LABEL = `${key}.header-cms-field-label`;
const HEADER_CSV_COLUMN_LABEL = `${key}.header-csv-column-label`;

export default {
  FORM_TITLE,
  HEADER_CMS_FIELD_LABEL,
  HEADER_CSV_COLUMN_LABEL,
};
