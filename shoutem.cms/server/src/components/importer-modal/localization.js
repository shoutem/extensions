const key = 'importer-modal';

const CSV_SOURCE_LABEL = `${key}.csv-source-label`;
const RSS_SOURCE_LABEL = `${key}.rss-source-label`;
const FORM_SOURCE_TITLE = `${key}.form-source-title`;
const FORM_SOURCE_EMPTY_PLACEHOLDER_LABEL = `${key}.form-source-empty-placeholder-label`;
const BUTTON_CANCEL_LABEL = `${key}.button-cancel-label`;
const BUTTON_NEXT_LABEL = `${key}.button-next-label`;
const BUTTON_CREATE_LABEL = `${key}.button-create-label`;
const MODAL_TITLE = `${key}.modal-title`;

export default {
  CSV_SOURCE_LABEL,
  RSS_SOURCE_LABEL,
  FORM_SOURCE_TITLE,
  FORM_SOURCE_EMPTY_PLACEHOLDER_LABEL,
  BUTTON_CANCEL_LABEL,
  BUTTON_NEXT_LABEL,
  BUTTON_CREATE_LABEL,
  MODAL_TITLE,
};
