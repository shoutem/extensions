const key = 'content-preview';

const HEADER_TITLE = `${key}.header-title`;
const HEADER_CATEGORY = `${key}.header-category`;
const HEADER_LANGUAGE = `${key}.header-language`;
const EMPTY_PLACEHOLDER_MESSAGE = `${key}.empty-placeholder-message`;

export default {
  HEADER_TITLE,
  HEADER_CATEGORY,
  HEADER_LANGUAGE,
  EMPTY_PLACEHOLDER_MESSAGE,
};
