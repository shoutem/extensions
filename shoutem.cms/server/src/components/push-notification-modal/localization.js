const key = 'push-notification-modal';

const MODAL_TITLE = `${key}.modal-title`;
const FORM_TITLE = `${key}.form-title`;
const FORM_MESSAGE = `${key}.form-message`;
const FORM_LANGUAGES = `${key}.form-languages`;
const BUTTON_SEND = `${key}.button-send`;
const BUTTON_CANCEL = `${key}.button-cancel`;
const GENERIC_ERROR = `${key}.generic-error`;

export default {
  MODAL_TITLE,
  FORM_TITLE,
  FORM_MESSAGE,
  FORM_LANGUAGES,
  BUTTON_SEND,
  BUTTON_CANCEL,
  GENERIC_ERROR,
};
