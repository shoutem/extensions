const key = 'importer-table';

const ADD_IMPORTER_BUTTON_LABEL = `${key}.add-importer-button-label`;
const HEADER_TITLE_LABEL = `${key}.header-title-label`;
const HEADER_STATUS_LABEL = `${key}.header-status-label`;
const HEADER_LAST_IMPORT_ON_LABEL = `${key}.header-last-import-on-label`;
const EMPTY_PLACEHOLDER_LABEL = `${key}.empty-placeholder-label`;

export default {
  ADD_IMPORTER_BUTTON_LABEL,
  HEADER_TITLE_LABEL,
  HEADER_STATUS_LABEL,
  HEADER_LAST_IMPORT_ON_LABEL,
  EMPTY_PLACEHOLDER_LABEL,
};
