const key = 'advanced-setup';

const TITLE = `${key}.title`;
const BUTTON_SAVE = `${key}.button-save`;
const BUTTON_CANCEL = `${key}.button-cancel`;

export default {
  TITLE,
  BUTTON_SAVE,
  BUTTON_CANCEL,
};
