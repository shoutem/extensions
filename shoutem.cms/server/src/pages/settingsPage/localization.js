const key = 'settings-page';

const TITLE = `${key}.title`;
const FORM_API_ENDPOINT = `${key}.form-api-endpoint`;
const BUTTON_SAVE = `${key}.button-save`;

export default {
  TITLE,
  FORM_API_ENDPOINT,
  BUTTON_SAVE,
};
