const key = 'services';

const ALL_CATEGORY = `${key}.all-category`;
const ALL_CATEGORIES = `${key}.all-categories`;
const MORE_CATEGORIES = `${key}.more-categories`;
const SELECT_DATA = `${key}.select-data`;

export default {
  ALL_CATEGORY,
  ALL_CATEGORIES,
  MORE_CATEGORIES,
  SELECT_DATA,
};
