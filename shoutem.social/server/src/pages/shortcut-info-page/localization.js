const key = 'shortcut-info-page';

const CONFIGURE_SETTINGS_TEXT = `${key}.configure-settings-text`;
const MANAGE_APP_USERS_TEXT = `${key}.manage-app-users-text`;

export default {
  CONFIGURE_SETTINGS_TEXT,
  MANAGE_APP_USERS_TEXT,
};
