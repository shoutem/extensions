const key = 'user-groups-table';

const EMPTY_PLACEHOLDER_MESSAGE = `${key}.empty-placeholder-message`;
const HEADER_NAME_TITLE = `${key}.header-name-title`;
const HELP_TEXT = `${key}.help-text`;

export default {
  EMPTY_PLACEHOLDER_MESSAGE,
  HEADER_NAME_TITLE,
  HELP_TEXT,
};
