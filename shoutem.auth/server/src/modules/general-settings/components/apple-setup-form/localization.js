const key = 'apple-setup-form';

const TITLE = `${key}.title`;
const FORM_APPLE_TITLE = `${key}.form-apple-title`;
const FORM_DARK_BUTTON_TITLE = `${key}.form-dark-button-title`;
const LEARN_MORE_MESSAGE = `${key}.learn-more-message`;
const APPLE_SIGN_IN_MESSAGE = `${key}.apple-sign-in-message`;
const FORM_APPLE_BUTTON_STYLE_TITLE = `${key}.form-apple-button-style-title`;
const DARK_BUTTON_TOOLTIP_MESSAGE = `${key}.dark-button-tooltip-message`;

export default {
  TITLE,
  FORM_APPLE_TITLE,
  FORM_DARK_BUTTON_TITLE,
  LEARN_MORE_MESSAGE,
  APPLE_SIGN_IN_MESSAGE,
  FORM_APPLE_BUTTON_STYLE_TITLE,
  DARK_BUTTON_TOOLTIP_MESSAGE,
};
