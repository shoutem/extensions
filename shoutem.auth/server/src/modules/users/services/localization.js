const key = 'services';

const USERNAME_EXISTS_MESSAGE = `${key}.username-exists-message`;
const EMAIL_EXISTS_MESSAGE = `${key}.email-exists-message`;
const ERROR_MESSAGE = `${key}.error-message`;
const USERNAME_REQUIRED_MESSAGE = `${key}.username-required-message`;
const USERNAME_TOO_SHORT_MESSAGE = `${key}.username-too-short-message`;
const EMAIL_REQUIRED_MESSAGE = `${key}.email-required-message`;
const EMAIL_INVALID_MESSAGE = `${key}.email-invalid-message`;
const PASSWORD_REQUIRED_MESSAGE = `${key}.password-required-message`;
const PASSWORD_TOO_SHORT_MESSAGE = `${key}.password-too-short-message`;

export default {
  USERNAME_EXISTS_MESSAGE,
  EMAIL_EXISTS_MESSAGE,
  ERROR_MESSAGE,
  USERNAME_REQUIRED_MESSAGE,
  USERNAME_TOO_SHORT_MESSAGE,
  EMAIL_REQUIRED_MESSAGE,
  EMAIL_INVALID_MESSAGE,
  PASSWORD_REQUIRED_MESSAGE,
  PASSWORD_TOO_SHORT_MESSAGE,
};
