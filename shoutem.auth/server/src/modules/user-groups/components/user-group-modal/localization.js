const key = 'user-group-modal';

const RENAME_GROUP_TITLE = `${key}.rename-group-title`;
const CREATE_GROUP_TITLE = `${key}.create-group-title`;
const FORM_GROUP_NAME_TITLE = `${key}.form-group-name-title`;
const BUTTON_SUBMIT_TITLE = `${key}.button-submit-title`;
const BUTTON_CANCEL_TITLE = `${key}.button-cancel-title`;

export default {
  RENAME_GROUP_TITLE,
  CREATE_GROUP_TITLE,
  FORM_GROUP_NAME_TITLE,
  BUTTON_SUBMIT_TITLE,
  BUTTON_CANCEL_TITLE,
};
