const key = 'user-groups-screen-settings';

const TITLE = `${key}.title`;
const SCREEN_VISIBILITY_HELP_MESSAGE = `${key}.screen-visibility-help-message`;
const HEADER_SCREEN_TITLE = `${key}.header-screen-title`;
const HEADER_VISIBLTE_TO_TITLE = `${key}.header-visible-to-title`;

export default {
  TITLE,
  SCREEN_VISIBILITY_HELP_MESSAGE,
  HEADER_SCREEN_TITLE,
  HEADER_VISIBLTE_TO_TITLE,
};
