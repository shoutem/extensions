const key = 'newsletter-settings';

const TITLE = `${key}.title`;
const CONSENT_TOGGLE_ACTIVE_TITLE = `${key}.consent-toggle-active-title`;
const CONSENT_TOGGLE_ACTIVE_TOOLTIP = `${key}.consent-toggle-active-tooltip`;

export default {
  TITLE,
  CONSENT_TOGGLE_ACTIVE_TITLE,
  CONSENT_TOGGLE_ACTIVE_TOOLTIP,
};
