const key = 'protected-screen-modal';

const IOS_PRODUCT_TITLE = `${key}.ios-product-title`;
const ANDROID_PRODUCT_TITLE = `${key}.android-product-title`;
const BUTTON_SEND = `${key}.button-send`;
const BUTTON_CANCEL = `${key}.button-cancel`;
const GENERIC_ERROR = `${key}.generic-error`;

export default {
  IOS_PRODUCT_TITLE,
  ANDROID_PRODUCT_TITLE,
  BUTTON_SEND,
  BUTTON_CANCEL,
  GENERIC_ERROR,
};
