const key = 'chat-disabled-placeholder';

const TITLE = `${key}.title`;
const ERROR = `${key}.error`;
const FEATURE_ENABLE_MESSAGE = `${key}.feature-enable-message`;
const BUTTON_LABEL_ERROR = `${key}.button-label-error`;
const BUTTON_LABEL_START = `${key}.button-label-start`;

export default {
  TITLE,
  ERROR,
  FEATURE_ENABLE_MESSAGE,
  BUTTON_LABEL_ERROR,
  BUTTON_LABEL_START,
};
