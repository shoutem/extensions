const key = 'fair-policy-description';

const POPOVER_MESSAGE = `${key}.popover`;
const DESCRIPTION = `${key}.description`;
const LEARN_MORE = `${key}.learn-more`;

export default {
  DESCRIPTION,
  POPOVER_MESSAGE,
  LEARN_MORE,
};
