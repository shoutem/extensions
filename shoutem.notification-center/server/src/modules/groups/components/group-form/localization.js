const key = 'group-form';

const FORM_NAME_INPUT_DESCRIPTION = `${key}.name-input-label`;
const FORM_IMAGE_LABEL = `${key}.image-picker-label`;
const FORM_IMAGE_HELP_TEXT = `${key}.image-help-text`;
const SUBSCRIBE_TOGGLE_TEXT = `${key}.subscribe-toggle-text`;
const BUTTON_SAVE = `${key}.button-save`;
const BUTTON_ADD = `${key}.button-add`;
const BUTTON_CANCEL = `${key}.button-cancel`;

export default {
  FORM_NAME_INPUT_DESCRIPTION,
  FORM_IMAGE_LABEL,
  FORM_IMAGE_HELP_TEXT,
  SUBSCRIBE_TOGGLE_TEXT,
  BUTTON_SAVE,
  BUTTON_ADD,
  BUTTON_CANCEL,
};
