const key = 'chime-uploader-fragment';

const CHIME_ALERT = `${key}.chime-alert`;
const ERROR_FILE_REJECT = `${key}.error-file-reject`;
const ERROR_INVALID_FILE_NAME = `${key}.error-invalid-file-name`;
const ERROR_MAX_SIZE = `${key}.error-max-size`;
const ERROR_UPLOAD_FAIL = `${key}.error-upload-fail`;
const SETTINGS_TITLE = `${key}.settings-title`;
const SOUND_LABEL = `${key}.sound-title`;

export default {
  CHIME_ALERT,
  ERROR_FILE_REJECT,
  ERROR_INVALID_FILE_NAME,
  ERROR_MAX_SIZE,
  ERROR_UPLOAD_FAIL,
  SETTINGS_TITLE,
  SOUND_LABEL,
};
