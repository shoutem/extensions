const key = 'notification-settings';

const DISABLED_LABEL = `${key}.disabled-label`;
const ENABLED_LABEL = `${key}.enabled-label`;
const REMINDER_LABEL = `${key}.reminder-label`;
const PN_SETTINGS_TITLE = `${key}.pn-settings-title`;
const REMINDER_MESSAGE_LABEL = `${key}.reminder-message-label`;
const SCHEDULED_NOTIFICATIONS_LABEL = `${key}.scheduled-notifications-label`;

export default {
  DISABLED_LABEL,
  ENABLED_LABEL,
  PN_SETTINGS_TITLE,
  REMINDER_LABEL,
  REMINDER_MESSAGE_LABEL,
  SCHEDULED_NOTIFICATIONS_LABEL,
};
