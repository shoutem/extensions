const key = 'notifications-services';

const VALUE_REQUIRED_TEXT = `${key}.value-required-text`;
const NUMBER_NOT_IN_RANGE_TEXT = `${key}.number-not-in-range-text`;

export default {
  VALUE_REQUIRED_TEXT,
  NUMBER_NOT_IN_RANGE_TEXT,
};
