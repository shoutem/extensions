const key = 'extension-page';

const TITLE = `${key}.title`;
const FORM_IOS = `${key}.form-ios`;
const FORM_ANDROID = `${key}.form-android`;
const BUTTON_SAVE = `${key}.button-save`;

export default {
  TITLE,
  FORM_IOS,
  FORM_ANDROID,
  BUTTON_SAVE,
};
