const key = 'icons-page';

const EMPTY_SHORTCUTS_PLACEHOLDER_TITLE = `${key}.empty-shortcuts-placeholder-title`;
const EMPTY_SHORTCUTS_PLACEHOLDER_MESSAGE = `${key}.empty-shortcuts-placeholder-message`;
const HEADER_NAVIGATION_ITEM = `${key}.header-navigation-item`;
const HEADER_ICON = `${key}.header-icon`;

export default {
  EMPTY_SHORTCUTS_PLACEHOLDER_TITLE,
  EMPTY_SHORTCUTS_PLACEHOLDER_MESSAGE,
  HEADER_NAVIGATION_ITEM,
  HEADER_ICON,
};
