const key = 'card-general-settings';

const CARD_HEIGHT_SMALL = `${key}.card-height-small`;
const CARD_HEIGHT_MEDIUM = `${key}.card-height-medium`;
const CARD_HEIGHT_LARGE = `${key}.card-height-large`;
const TITLE = `${key}.title`;
const FORM_ITEM_HEIGHT = `${key}.form-item-height`;
const FORM_FULL_WIDTH = `${key}.form-full-width`;

export default {
  CARD_HEIGHT_SMALL,
  CARD_HEIGHT_MEDIUM,
  CARD_HEIGHT_LARGE,
  TITLE,
  FORM_ITEM_HEIGHT,
  FORM_FULL_WIDTH,
};
