const key = 'card-icon-background-settings';

const TITLE = `${key}.title`;
const HEADER_ICON_BACKGROUNDS = `${key}.header-icon-backgrounds`;
const HEADER_NORMAL = `${key}.header-normal`;

export default {
  TITLE,
  HEADER_ICON_BACKGROUNDS,
  HEADER_NORMAL,
};
