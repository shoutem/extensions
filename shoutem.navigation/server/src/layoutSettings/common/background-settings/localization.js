const key = 'background-settings';

const TABLET_SCREEN_BACKGROUND = `${key}.tablet-screen-background`;
const SCREEN_BACKGROUND = `${key}.screen-background`;
const TITLE = `${key}.title`;

export default {
  TABLET_SCREEN_BACKGROUND,
  SCREEN_BACKGROUND,
  TITLE,
};
