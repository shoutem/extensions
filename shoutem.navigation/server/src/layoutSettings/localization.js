const key = 'layout-settings';

const GUTTER_NO_GUTTER = `${key}.gutter-no-gutter`;
const GUTTER_SMALL = `${key}.gutter-small`;
const GUTTER_MEDIUM = `${key}.gutter-medium`;
const GUTTER_LARGE = `${key}.gutter-large`;
const TEXT_NO_TEXT = `${key}.text-no-text`;
const TEXT_TOP_LEFT = `${key}.text-top-left`;
const TEXT_TOP_CENTER = `${key}.text-top-center`;
const TEXT_TOP_RIGHT = `${key}.text-top-right`;
const TEXT_MIDDLE_LEFT = `${key}.text-middle-left`;
const TEXT_MIDDLE_CENTER = `${key}.text-middle-center`;
const TEXT_MIDDLE_RIGHT = `${key}.text-middle-right`;
const TEXT_BOTTOM_LEFT = `${key}.text-bottom-left`;
const TEXT_BOTTOM_CENTER = `${key}.text-bottom-center`;
const TEXT_BOTTOM_RIGHT = `${key}.text-bottom-right`;
const TEXT_SIZE_SMALL = `${key}.text-size-small`;
const TEXT_SIZE_MEDIUM = `${key}.text-size-medium`;
const TEXT_SIZE_LARGE = `${key}.text-size-large`;
const FORM_TEXT_SIZE = `${key}.form-text-size`;
const FORM_GUTTER_SETTINGS = `${key}.form-gutter-settings`;
const FORM_TEXT_SETTINGS = `${key}.form-text-settings`;

export default {
  GUTTER_NO_GUTTER,
  GUTTER_SMALL,
  GUTTER_MEDIUM,
  GUTTER_LARGE,
  TEXT_NO_TEXT,
  TEXT_TOP_LEFT,
  TEXT_TOP_CENTER,
  TEXT_TOP_RIGHT,
  TEXT_MIDDLE_LEFT,
  TEXT_MIDDLE_CENTER,
  TEXT_MIDDLE_RIGHT,
  TEXT_BOTTOM_LEFT,
  TEXT_BOTTOM_CENTER,
  TEXT_BOTTOM_RIGHT,
  TEXT_SIZE_SMALL,
  TEXT_SIZE_MEDIUM,
  TEXT_SIZE_LARGE,
  FORM_TEXT_SIZE,
  FORM_GUTTER_SETTINGS,
  FORM_TEXT_SETTINGS,
};
