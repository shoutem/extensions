const key = 'monitored-screens-table';

const TITLE = `${key}.title`;
const TABLE_EMPTY_TEXT = `${key}.table-empty-text`;
const TABLE_HEADER_SCREEN = `${key}.table-header-screen`;

export default {
  TITLE,
  TABLE_EMPTY_TEXT,
  TABLE_HEADER_SCREEN,
};
