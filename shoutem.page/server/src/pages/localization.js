const key = 'page-settings';

const TITLE = `${key}.title`;
const FORM_CHOOSE_LAYOUT = `${key}.form-choose-layout`;
const LAYOUT_TYPE_ICON_GRID = `${key}.layout-type-icon-grid`;
const LAYOUT_TYPE_LIST = `${key}.layout-type-list`;

export default {
  TITLE,
  FORM_CHOOSE_LAYOUT,
  LAYOUT_TYPE_ICON_GRID,
  LAYOUT_TYPE_LIST,
};
