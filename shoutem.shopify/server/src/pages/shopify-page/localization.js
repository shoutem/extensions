const key = 'shopify-page';

const PREVIOUS_PAGE = `${key}.previous-page`;
const NEXT_PAGE = `${key}.next-page`;
const FORM_TITLE = `${key}.form-title`;
const FORM_DESCIRPTION = `${key}.form-description`;
const SHOP_NOT_CONFIGURED = `${key}.shop-not-configured-description`;
const SHOP_NOT_CONFIGURED_LINK = `${key}.shop-not-configured-description-link`;

export default {
  PREVIOUS_PAGE,
  NEXT_PAGE,
  FORM_TITLE,
  FORM_DESCIRPTION,
  SHOP_NOT_CONFIGURED,
  SHOP_NOT_CONFIGURED_LINK,
};
