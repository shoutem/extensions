const key = 'shopify-page';

const PREVIOUS_PAGE = `${key}.previous-page`;
const NEXT_PAGE = `${key}.next-page`;
const FORM_TITLE = `${key}.form-title`;
const FORM_DESCIRPTION = `${key}.form-description`;

export default {
  PREVIOUS_PAGE,
  NEXT_PAGE,
  FORM_TITLE,
  FORM_DESCIRPTION,
};
