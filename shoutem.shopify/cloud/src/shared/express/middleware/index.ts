import favicon from './favicon';
import { asyncMiddleware } from './async-middleware';
import asyncParamMiddleware from './async-param-middleware';

export { favicon, asyncMiddleware, asyncParamMiddleware };
