const key = 'feed-preview';

const LOADING_FROM = `${key}.loading-from`;
const DATE = `${key}.date`;
const TITLE = `${key}.title`;
const LOADING = `${key}.loading`;
const NO_CONTENT = `${key}.no-content`;
const CHOOSE_FROM_FEEDS = `${key}.choose-from-feeds`;
const ADD_FEED = `${key}.add-feed`;
const CANCEL_BUTTON = `${key}.cancel-button`;
const INVALID_URL = `${key}.invalid-url`;
const ENTER_LINK = `${key}.enter-link`;
const CONTINUE_BUTTON = `${key}.continue-button`;
const NO_VALID_FEED = `${key}.no-valid-feed`;

export default {
  LOADING_FROM,
  DATE,
  TITLE,
  LOADING,
  NO_CONTENT,
  CHOOSE_FROM_FEEDS,
  ADD_FEED,
  CANCEL_BUTTON,
  INVALID_URL,
  ENTER_LINK,
  CONTINUE_BUTTON,
  NO_VALID_FEED,
};
