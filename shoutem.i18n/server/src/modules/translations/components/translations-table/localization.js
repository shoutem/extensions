const key = 'translations-table';

const HEADER_CODE = `${key}.header-code`;
const HEADER_LANGUAGE = `${key}.header-language`;
const HEADER_STATUS = `${key}.header-status`;
const STATUS_ACTIVE = `${key}.status-active`;
const STATUS_INACTIVE = `${key}.status-inactive`;
const TABLE_PLACEHOLDER = `${key}.table-placeholder`;

export default {
  HEADER_CODE,
  HEADER_LANGUAGE,
  HEADER_STATUS,
  STATUS_ACTIVE,
  STATUS_INACTIVE,
  TABLE_PLACEHOLDER
};
