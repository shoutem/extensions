const key = 'language-page';

const TITLE = `${key}.title`;
const FORM_TITLE = `${key}.form-title`;
const DESCRIPTION = `${key}.description`;
const TUTORIAL_LINK = `${key}.tutorial-link`;

export default {
  TITLE,
  FORM_TITLE,
  DESCRIPTION,
  TUTORIAL_LINK,
}
