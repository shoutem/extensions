const key = 'screens-page';

const SELECT_LANGUAGE_LABEL = `${key}.select-language-label`;
const NO_LANGUAGES_ADDED_LABEL = `${key}.no-languages-added-label`;
const BUTTON_TEXT = `${key}.button-text`;

export default {
  SELECT_LANGUAGE_LABEL,
  NO_LANGUAGES_ADDED_LABEL,
  BUTTON_TEXT
}
